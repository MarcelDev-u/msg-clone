services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8600:8080"
    environment:
      CHAT_DB_PATH: /data/chat.db
    volumes:
      - chat-data:/data

volumes:
  chat-data:
